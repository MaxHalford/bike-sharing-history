[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh scripts/railway-scrape.sh"
cronSchedule = "1,16,31,46 * * * *"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 1
