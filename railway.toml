[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh scripts/railway-scrape.sh"
cronSchedule = "1,13,25,37,49 * * * *"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 1
